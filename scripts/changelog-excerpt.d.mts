export function changelogSection(text: string, version: string): { date: string; body: string };
export function run(argv: string[], options?: { cwd?: string; log?: (line: string) => void }): number;
